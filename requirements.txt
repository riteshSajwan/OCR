pymupdf
opencv-python
numpy
pillow
pytesseract
pyyaml

# The TrOCR engine's dependencies are in requirements-trocr.txt - they are large
# and only needed for `--engine trocr`, so they are kept out of the default install.
